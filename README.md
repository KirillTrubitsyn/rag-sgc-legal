# RAG SGC Legal

Юридический ассистент для работы с нормативными документами СГК (Сибирская генерирующая компания).

## Технологии
- Next.js 15 + React 19 + TypeScript
- xAI Grok 4.1 Fast + Collections (hybrid search)
- Vercel AI SDK (streaming)
- Tailwind CSS

## Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка переменных окружения

Создай файл `.env`:

```env
XAI_API_KEY=your_xai_api_key_here
COLLECTION_ID=your_collection_id_here
```

**Где взять:**
- **XAI_API_KEY**: создай API ключ "RAG SGC" на https://console.x.ai
- **COLLECTION_ID**: скопируй ID коллекции "Standards and Regulations" на https://console.x.ai/collections

### 3. Запуск dev сервера

```bash
npm run dev
```

Открой http://localhost:3000

## Deploy на Vercel

1. Push код в GitHub
2. Импортируй репозиторий на https://vercel.com
3. Добавь Environment Variables: `XAI_API_KEY`, `COLLECTION_ID`
4. Deploy

Каждый последующий `git push` автоматически обновит production.

## Структура проекта

```
rag-sgc-legal/
├── app/
│   ├── api/
│   │   └── chat/
│   │       └── route.ts          # Streaming API endpoint
│   ├── components/
│   │   └── chat/
│   │       └── ChatInterface.tsx  # Главный UI компонент чата
│   ├── layout.tsx                 # Root layout
│   ├── page.tsx                   # Главная страница
│   └── globals.css                # Глобальные стили
├── lib/
│   ├── xai-client.ts              # xAI клиент + Grok модель
│   └── utils.ts                   # Утилиты (cn)
└── .env                           # Переменные окружения
```

## Возможности

### 🔍 Гибридный поиск
- Семантический и ключевой поиск по документам
- Интеграция с xAI Collections API
- Релевантность результатов с оценкой score

### 💬 Streaming чат
- Ответы в реальном времени с использованием AI SDK
- Многошаговые рассуждения (maxSteps: 5)
- Отображение источников и метаданных документов

### 🎨 Современный UI
- Адаптивный дизайн (mobile-first)
- Tailwind CSS стилизация
- Empty state и loading состояния
- Карточки с результатами поиска

## Примеры запросов

- "Найди информацию о порядке согласования договоров"
- "Какие штрафные санкции предусмотрены в стандартах СГК?"
- "Покажи требования к оформлению протоколов разногласий"
- "Есть ли информация о сроках хранения документов?"

## API Endpoints

### POST /api/chat

Streaming endpoint для общения с ассистентом.

**Request:**
```json
{
  "messages": [
    {
      "role": "user",
      "content": "Какие требования к охране труда?"
    }
  ]
}
```

**Response:**
- Streaming ответ в формате AI SDK
- Tool invocations с результатами поиска
- Метаданные документов (source, page, score)

## Разработка

```bash
npm run dev      # Development server
npm run build    # Production build
npm run start    # Production server
npm run lint     # ESLint
```

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `XAI_API_KEY` | API ключ xAI | ✅ |
| `COLLECTION_ID` | ID коллекции документов | ✅ |

## Лицензия

MIT
